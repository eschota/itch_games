# UnSoccer v0.0.003 Acceptance Gate

Date: 2026-07-01
Role: Programmer / Orchestrator
Task: `task_programmer_game_engine`

## Scope

- Runtime under validation: `unsoccer/shared`, `unsoccer/server`,
  `unsoccer/client`.
- Tool added: `tools/unsoccer_acceptance.mjs`.
- Script added: `npm run test:unsoccer:acceptance`.
- Test server mode: isolated `UNSOCCER_TEST_MODE=1` server on a temporary
  loopback port with `UNSOCCER_LOCAL_ICE=1`.

## Validation

- `npm run typecheck:unsoccer`: PASS.
- `npm run test:unsoccer:acceptance`: PASS.
- `npm run package:unsoccer`: PASS, wrote `dist/unsoccer-itch.zip` at 143.1
  KiB.
- `git diff --check` for touched Programmer/source/docs scope: PASS.
- XML parse for `skill.xml` and `unsoccer/skill.xml`: PASS before the final
  rule-link update; repeat before release freeze.

## Acceptance Coverage

The deterministic gate verified:

- `/api/health` reports `v0.0.003`.
- Five test clients become four active players plus one spectator.
- Spectator has `team === null`.
- Left-foot kick changes `lastAction` and moves the ball.
- Right-foot kick changes `lastAction` and moves the ball.
- Head hit changes `lastAction` and moves the ball.
- Body contact changes `lastAction` and moves the ball.
- Goal scoring increments the correct score, resets the ball to center,
  starts countdown, and respawns players on their halves.

## Remaining Release Risks

- In-app Browser failed to create a new tab during this pass
  (`webview attach` timeout), so browser smoke against the fresh `8793` server
  was not completed here.
- Production/live deployment must still be retested after freeze.
- Visual acceptance still needs final screenshots for visible sun and separate
  body-vs-leg contact readability.
- Real user-activation audio unlock still needs a trusted browser gesture check.
